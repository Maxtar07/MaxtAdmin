module.exports = (bot) => {
    //    const channel = bot.channels.cache.get(config.log_channel_id);
    //    const processs = require(`node:process`);
    //
    //    processs.on(`uncaughtException`, (reason, promise) => {
    //        channel.send({ content: `Unhandle Rejection at: ${promise}\n---\nreason: ${reason}` });
    //    });
    //    processs.on(`unhandleRejection`, (err) => {
    //        channel.send({ content: `Uncaught Exception: ${err}` });
    //    });
    //    processs.on(`uncaughtExceptionMonitor`, (err, origin) => {
    //        channel.send({ content: `Uncaught exception Monitor: ${err}, ${origin}` });
    //    });
}